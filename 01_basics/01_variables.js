const accountId = 242342

let accountName = "suraj"
let cityName;

//console.log(accountId, accountName)

console.table([accountId, accountName,cityName])

/*
prefer to not use var
because of issue in block scope and functional scope
*/